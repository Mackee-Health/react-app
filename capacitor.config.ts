import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mackeehealth.app',
  appName: 'Mackee Health',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
